# [RUN_INPUT_PAYLOAD]

> **INSTRUCTIONS FOR LLM**: Tuân thủ nghiêm ngặt định dạng bên dưới. Điền thông tin vào các block tương ứng. Phân biệt rõ ràng giữa nguyên bản yêu cầu và bản tóm tắt. Nếu không có ghi chú thêm, điền `None`.

## 1. RUN_METADATA
- **Run_ID**: [Điền mã định danh hệ thống, ví dụ: chuỗi Run_ID]

## 2. RAW_USER_REQUEST
[Trích dẫn nguyên bản (Verbatim) yêu cầu của người dùng. RÀNG BUỘC: Tuyệt đối KHÔNG chỉnh sửa, không diễn giải lại, không sửa lỗi chính tả hay định dạng của người dùng.]

## 3. INTENT_BRIEF
[Tóm tắt giữ nguyên ý định (Intent-preserving summary). RÀNG BUỘC: Bóc tách và giữ lại chính xác mục tiêu cốt lõi của người dùng, loại bỏ các câu từ dư thừa hoặc cảm xúc. Súc tích]

## 4. ADDITIONAL_NOTES
* [Lưu ý bổ sung, các tham số (parameters) ngầm định được rút ra từ ngữ cảnh, hoặc các ngoại lệ cần quan tâm.]
* [Nếu không có lưu ý nào, điền `None`.]
