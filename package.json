{
  "name": "codex-task-dispatch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/check-static-assets.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
